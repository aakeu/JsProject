let person = 
{
    name: "UncleBola",
    age: 18,
    country: "Nigeria",
    // logData: function()
    // {
    //     console.log(person.name + " is " + person.age + " years old and live in " + person.country)
    // }
}
// person.logData();

function logData()
{
    console.log(person.name + " is " + person.age + " years old and live in " + person.country)
}
logData();